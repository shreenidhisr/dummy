# Graph Representation (Remotion)

Remotion animation project lives in [`remotion-graph/`](./remotion-graph).

```bash
cd remotion-graph
npm install
npm run dev
```

Composition: `GraphRepresentation` (~19s) — visual graph → adjacency list → adjacency matrix.

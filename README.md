# Graph Representation (Remotion)

Remotion animation in [`remotion-graph/`](./remotion-graph):

- Sample graph
- Adjacency list + adjacency matrix (the two representations)
- Differences
- Java code for both

```bash
cd remotion-graph && npm install && npm run dev
```
